(defwidget battery []
  (eventbox  :class "battery" :onclick "uwsm app -- kitty --title=specialtty -e btop &" :tooltip "${EWW_BATTERY.BAT0.status}"
    (label :class "battery label" :halign "center" :text "${EWW_BATTERY.BAT0.capacity}%")
  )
)
