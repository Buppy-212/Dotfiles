(defwidget idle []
  (box :orientation "h"
    (button :class "idle-button" :onclick "scripts/idle.sh --toggle" :tooltip "Idle Inhibitor"
      (label :class "idle-icon" :text idle)
    )
  )
)

(defwidget vidle []
  (box :orientation "v"
    (button :class "vidle-button" :onclick "scripts/idle.sh --toggle" :tooltip "Idle Inhibitor"
      (label :class "idle-icon" :text idle)
    )
  )
)
