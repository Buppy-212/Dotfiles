(defwidget gamemode []
  (box :orientation "h"
    (button :class "gamemode-button" :onclick "scripts/gamemode.sh --toggle" :tooltip "Gamemode"
      (label :class "gamemode-icon" :text gamemode)
    )
  )
)

(defwidget vgamemode []
  (box :orientation "v"
    (button :class "vgamemode-button" :onclick "scripts/gamemode.sh --toggle" :tooltip "Gamemode"
      (label :class "gamemode-icon" :text gamemode)
    )
  )
)
