(defwidget bluetooth []
  (box :orientation "h"
    (button :onclick "uwsm app -- ghostty --title=bluetui -e bluetui &" :class "bluetooth-button"
      (label :text "󰂯" :class "bluetooth-icon")
    )
  )
)
