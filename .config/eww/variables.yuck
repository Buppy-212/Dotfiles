(defpoll updates :initial "{\"tooltip\":\"Updated\",\"text\":\" \"}" :interval "15m" "scripts/updates.sh")
(defpoll volume :initial 0 :interval "1s" "pamixer --get-volume")
(defpoll wifi-icon :interval "1m" "scripts/wifi.sh icon")
(defpoll wifi-name :interval "1m" "scripts/wifi.sh name")
(deflisten workspaces :initial "[]" "scripts/workspaces.sh")
(deflisten current_workspace :initial "1" "scripts/active-workspace.sh")
(defvar revealPower false)
(defvar revealVolume false)
